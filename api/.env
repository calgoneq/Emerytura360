# ---- Tryb / bezpieczeństwo ----
DEMO=1                          # 1 = dosiej średnie, jeśli brak pliku (tryb demo/hackathon)
ADMIN_KEY=sekret123_!Zm1En      # (na przyszłość; obecnie nieużywany w kodzie)

# ---- Ekonomia / modele wzrostu ----
WAGE_GROWTH=0.03                # fallback: średni roczny wzrost płac (gdy brak ścieżki z PARAMS)
CPI=0.03                        # fallback CPI (gdy w PARAMS dla bieżącego roku brak cpi_index)
AUTO_BACKCAST=1                 # 1 = automatyczny backcast płac w przeszłość
AVERAGES_FALLBACK_GROWTH=0.03   # CAGR do ekstrapolacji średnich emerytur poza lata z pliku

# ---- PDF (gałki layoutu wykresu) ----
PDF_CHART_H=110                 # wysokość wykresu słupkowego (px)
PDF_CHART_SIDE_PAD=0            # dodatkowy wewnętrzny margines z lewej/prawej (px)
